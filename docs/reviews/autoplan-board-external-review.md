# Autoplan Board External Review Log

This file records the required pre-PR review state for high-risk slices.

## Current Status

- S01 Private-Repo Safety Gate: Codex implemented and local tests cover public-repo fixture-only behavior.
- S03 Runtime State And Audit Store: Codex implemented runtime JSONL state tests.
- S07 Safe Broker Core: Codex implemented allowlisted action and abuse-case tests.
- S09 Autoplan Controller Sidecars: v1 sidecar metadata is represented in the Autoplan Review board and remains local-only.
- S10 Autonomous Prospect Chain Kickoff: v1 is fixture-only and pauses at HITL/external gates.
- S12 Audit And Evidence Gate: ship gate runs dashboard verify, browser smoke capture, npm audit, and evidence freshness/PNG consistency checks under `evidence/autoplan/`.
- S14 Telegram Control Plane: Codex implemented allowlist/raw-command-block tests, read-only HTTP shim protection, and poller approval coverage.
- S15 Full Local Integration Gate: local gates pass when `npm run ship-gate` is rerun after browser smoke; PR readiness remains blocked only on Gemini auth being unavailable and GitHub human review approval.

## Review Packets

- Claude Code terminal: completed read-only review with `claude -p ... --permission-mode plan` after sandbox escalation allowed the normal auth context. Initial findings:
  - H1 malformed JSON broker POST could hang: fixed with `readJsonBody()` and 400 `invalid-json` responses, covered in `tools/autoplan-board/tests/server.test.mjs`.
  - H2 JSON CORS header allowed `Origin: null`: fixed by omitting `access-control-allow-origin`, covered in server tests.
  - H3 missing Telegram HTTP endpoint: fixed with `POST /api/telegram`, covered in server tests.
  - H4 no server-level broker tests: fixed with CSRF, origin, invalid JSON, valid action, and abuse guard coverage.
  - M4 absolute broker args allowed: fixed with absolute-path validation and tests.
  - M5 Telegram approval target freeform: fixed with target validation against `S##` / `job-######` formats and tests.
  - M6 invalid ARIA list semantics: fixed by removing the incorrect `role="list"` and later using semantic evidence `<ul>/<li>`.
  - M7 focusable card without activation: fixed with an `Open detail` button and JS toggle behavior.
  Re-review found the original blocker classes resolved and one medium remaining issue: `/api/telegram` lacked CSRF/origin guard. That was fixed by applying the same token/origin checks as broker, and server tests now cover both.
  Final Claude plan-mode re-review found no blocking findings. Its only real correctness gap was a low-severity dangling `refreshBoard` broker target; fixed by adding `tools/autoplan-board/scripts/refresh-board.mjs` and test coverage. Claude also noted two info-level display/DOM guards, both fixed.
  A later Claude review of the broker execution/runtime bridge pass found one medium issue: completed broker actions stayed permanently deduped for the server lifetime. Fixed by releasing the dedupe key in `executeBrokerJob()` cleanup and adding rerun regression coverage. Narrow Claude re-review confirmed the medium finding resolved with no new blocker.
  Claude Desktop was then prompted through Computer Use for a full read-only review of the complete Codex work. It found four high blockers and several medium issues:
  - B1 dashboard UI could not invoke broker because the random session token was never delivered to the browser.
  - B2 `shipGate` was only an alias to `verify`.
  - B3 slice status was hardcoded to `implemented`.
  - B4 Telegram control was only a local HTTP shim, not a real bot integration path.
  - Medium issues included absent-Origin handling, ignored args on fixed-argv actions, hardcoded GitHub repo configuration, fixture-chain overclaiming, and no scaffold execution test.
  Codex fixed those by adding browser broker controls and token meta wiring, a real root `scripts/ship-gate.mjs`, derived slice status, required Origin handling, no-arg rejection for fixed-argv actions, configurable `AUTOPLAN_GITHUB_REPO`, real Telegram polling via `getUpdates` when configured, and scaffold execution coverage.
  Narrow Claude terminal re-review after commit `82d0bb1` confirmed all 10 prior Claude Desktop findings resolved with no blocking findings remaining. It noted two non-blocking caveats: Telegram polling callback should eventually add retry/error handling, and `AUTOPLAN_GITHUB_REPO` still has an own-project fallback default.
  Claude Desktop was prompted again through Computer Use against head commit `4eda291` after the external-review docs were recorded. It reported PR-ready code state with no blocking findings, and identified two optional follow-ups: wrap Telegram polling failures in audit-backed error handling, and replace slice-status file-existence fallbacks with evidence-backed derivation. Codex fixed both by recording `telegram.poll.failed` audit events from the poller, tightening S09/S10/S15 derivation to require runtime sidecar/chain/ship-gate evidence, adding regression tests, and making `scripts/ship-gate.mjs` stamp fixture integration evidence under `.autoplan-board/`.
  Claude Desktop was then prompted through Computer Use against the fresh uncommitted final patch set. It verified the targeted fixes, reported no blockers, and marked the code surface/evidence merge-ready. It raised two non-gating medium notes: ship-gate evidence should make the source of runtime jobs visible, and `board-refresh.json` should avoid independent writer drift. Codex addressed both by surfacing source-qualified chain evidence such as `verify: succeeded (ship-gate)` and moving board-refresh summary construction into a shared `buildBoardRefreshSummary()` helper used by both refresh paths.
  Claude Desktop was then prompted through Computer Use against the pushed PR head `6a8e394` (`Address Kimi final hardening notes`). It verified `processBrokerRequest()` catches broker registry drift and returns `broker-action-unavailable`, the server catch-all logs `server.request.failed` while returning generic `internal-error`, source-qualified fixture-chain evidence, and shared `buildBoardRefreshSummary()` use from both refresh writers. It reported no medium or blocking findings; only low defense-in-depth notes remained, and it identified CODEOWNERS approval as the sole outstanding process gate.
- Kimi / KimiClaw: attempted with `kimi --print --plan`; blocked first by `PermissionError: [Errno 1] Operation not permitted: '/Users/kylemetzger/.kimi/logs/kimi.log'`. Escalated retry then failed before review with `Unknown error: Failed to connect MCP servers: {'auto-browser': RuntimeError('Client failed to connect: Connection closed')}` and left stuck review processes, which Codex stopped (`83161`, `83172`). No Kimi edits were made.
  KimiClaw was then prompted through the Kimi desktop UI. Its earlier review findings included stale PR/audit/script concerns, and a follow-up UI note questioned whether every broker action maps to a real command surface. Codex added `broker registry maps every local action to an existing command surface` coverage in `tools/autoplan-board/tests/core.test.mjs`; after later hardening, the package test now passes 22/22.
  OpenClaw-FL5 re-reviewed PR #50 head commit `57ddf9b` in the Kimi UI and reported no blocking findings. It marked privacy, broker guards, Telegram allowlist/CSRF/origin/polling, agent-board read-only behavior, S01-S15 evidence coverage, and PR readiness as passing. It called out only two process-level gaps: Gemini remains blocked by interactive auth, and GitHub still requires human review approval.
  Kimi desktop was prompted again through Computer Use for the final patch. After the commit was pushed, Kimi produced a GitHub-visible final report with no blocking findings. It raised two medium hardening notes around uncontrolled broker registry drift and server 500 responses echoing `error.message`; Codex fixed both by returning `broker-action-unavailable` from broker request validation and removing internal exception messages from HTTP 500 responses while logging details to the local audit store.
  Kimi desktop was then prompted through Computer Use again against the actual pushed PR head `6a8e394`, after Kimi Conductor corrected the stale `57ddf9b` target. Kimi Claw and OpenClaw-FL5 both reported no blocking findings on the 7-blocker audit. OpenClaw-FL5 specifically confirmed: privacy/public-repo safety passes; broker abuse guards pass with registry drift caught at `core.mjs`; Telegram allowlist, CSRF, Origin, and polling pass with `telegram.poll.failed` audit coverage; agent-board bridge remains read-only/no-relaunch; S01-S15 evidence coverage passes with source-qualified evidence; per-slice audit sufficiency is non-blocking for v1; PR #50 exists, CI is green, package tests pass, and only Gemini auth plus human GitHub review approval remain as process gaps.
  Kimi desktop was prompted once more through Computer Use after the final pushed head `a475788` and the 22/22 package-test state. The late Kimi packet reported no blockers and marked commit/push as not blocked, but listed four medium concerns for adversarial adjudication. Codex checked them against the actual tree: `verifyGitHubRepoPrivacy()` fail-closes with `repo-privacy-verification-failed` or `repo-privacy-verification-invalid` and does not persist `gh` stderr; broker and direct `scripts/screenshot.js` output share the same allowlist validator for `evidence/autoplan/<safe>.json` and reject traversal, hidden files, non-evidence paths, and reserved evidence filenames; dedupe keys are released on privacy/lock rejection and in the execution `finally`; HTTP write blocking is applied at the `/api/broker` boundary for the shared `WRITE_ACTIONS` set, while Telegram polling remains a server-side allowlist-authenticated path by design. Codex also probed the direct screenshot CLI: `--output evidence/autoplan/../../.env` and `--output evidence/autoplan/.env.local` both returned `screenshot-output-not-allowed`, while a valid `evidence/autoplan/screenshot-request.json` metadata write succeeded. No Kimi medium was accepted as merge-blocking; the remaining notes are defense-in-depth or already-covered by tests.
- Codex adversarial subagents: dispatched code, security, test-strategy, and fresh-agent wiring audits against the local follow-up patch. They found blockers around untracked runbook links, stale/fake screenshot evidence acceptance, unsafe `screenshotAudit` output paths, direct screenshot metadata output paths, privacy-gate enforcement, token-only GitHub privacy verification, HTTP-injected Telegram approvals, misleading command documentation, locally fetchable HTTP broker-token risk, unverified repo-privacy assertions, and implicit GitHub Actions token permissions. Codex fixed those by tracking and linking `docs/AUTOPLAN_BOARD.md`, adding `dashboard:smoke`/`dashboard:refresh`/`dashboard:test`, adding real Playwright browser smoke capture, hardening `ship-gate` PNG/freshness/board-consistency checks, restricting broker and direct CLI screenshot metadata output and URL targets, preserving GitHub token env only for fixed-argv privacy lookup, blocking broker write actions when public non-fixture mode is active or privacy is unverified, disabling HTTP-triggered broker writes by default, making `/api/telegram` read-only for mutating commands, pinning workflow permissions to `contents: read`, running package verify/test plus ship-gate in CI/Verify workflows, and updating the runbook/review docs.
- Gemini CLI: attempted with `gemini --prompt ... --approval-mode plan`; blocked by interactive authentication prompt. A later retry through the user's shell init path still fell back to browser authentication, and a non-secret keychain check returned no `GEMINI_API_KEY` item (`status:44`). The prompt was not approved. No Gemini edits were made.

The remaining external-review gap is Gemini CLI authentication. It is documented as an unavailable lane, not a code blocker. GitHub still requires human review approval before merge.
